# Cloudflare Migration: Current State (May 6, 2026)

## 🎯 Goal
Migrate the Next.js 16 application to **Cloudflare Workers** using `@opennextjs/cloudflare`.
Target: **adwalididi-website** Worker (NOT Cloudflare Pages).

## ✅ Completed — Architecture

| Area | Status | Details |
|---|---|---|
| `open-next.config.ts` | ✅ Done | Uses `defineCloudflareConfig()` |
| `next.config.mjs` | ✅ Done | Has `initOpenNextCloudflareForDev()` |
| `wrangler.worker.jsonc` | ✅ Done | Points to `.open-next/worker.js` (Workers, not Pages) |
| GitHub Actions | ✅ Done | Deploys via `wrangler deploy --config wrangler.worker.jsonc` |
| `cf:build` / `cf:deploy` scripts | ✅ Done | In `package.json` |

## ✅ Completed — Runtime Compatibility

| Area | Status | Details |
|---|---|---|
| Blog Registry | ✅ Done | `lib/blog-registry.ts` — auto-generated by `npm run prebuild` from MDX files. gitignored. |
| Reviews Registry | ✅ Done | `lib/reviews-registry.ts` — auto-generated by `npm run prebuild` from `data/reviews.json`. gitignored. |
| Admin Gate (`/admin/open-gate`) | ✅ Done | Handles GET+POST, sets `admin_gate` cookie at `path: '/'` |
| Admin Login (`/api/auth/login`) | ✅ Done | Sets `admin_session` cookie |
| Admin Auth Check (`/api/auth/check`) | ✅ Done | Returns cookie status |
| Logout cookie path fix | ✅ Done | `admin_gate` now deleted with `path: '/'` (was `/admin/` — mismatch) |
| All routes use `runtime = 'edge'` | ✅ Done | Compatible with Workers |
| `data/reviews.json` | ✅ Committed | Fetched via `scripts/fetch-reviews.mjs`, committed to repo |

## 🚧 Remaining Steps for Production

### 1. Add Cloudflare Worker Secrets
Run these in WSL terminal (`npx wrangler secret put <NAME> --config wrangler.worker.jsonc`):
- `ADMIN_PASSWORD` (for `/admin/open-gate`)
- `ADMIN_USER_ID` (for `/api/auth/login`)
- `ADMIN_USER_PASSWORD` (for `/api/auth/login`)
- `RESEND_API_KEY`
- `GEMINI_API_KEY`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `NEXT_PUBLIC_SUPABASE_URL` (public — set in wrangler.worker.jsonc `vars`)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` (public — set in wrangler.worker.jsonc `vars`)

### 2. Update Worker Name for Production
The `wrangler.worker.jsonc` currently uses `"name": "adwalididi-website-preview"`.
For the final production cutover, update to `"name": "adwalididi-website"` and update the `WORKER_SELF_REFERENCE` service binding to match.

### 3. Local Build (WSL)
```bash
sudo rm -rf node_modules   # clean up Windows symlinks
npm install                 # install Linux binaries
npm run cf:build            # generates registries + builds worker
```

### 4. Deploy
```bash
npm run cf:deploy
# OR via GitHub Actions push to main/experimental
```

## 📂 Key Files
- `open-next.config.ts` — OpenNext Cloudflare config
- `wrangler.worker.jsonc` — Wrangler config for the Worker
- `scripts/build-blog-data.mjs` — Generates `lib/blog-registry.ts`
- `scripts/build-reviews-data.mjs` — Generates `lib/reviews-registry.ts`
- `lib/blog-data.ts` + `lib/reviews-data.ts` — Clean import interfaces
- `app/api/auth/login/route.ts` — Admin login (edge runtime)
- `app/api/auth/check/route.ts` — Auth cookie check (edge runtime)
- `app/admin/open-gate/route.ts` — Secret gate (edge runtime)
- `.github/workflows/cloudflare-build.yml` — CI/CD pipeline
